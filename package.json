{
    "name": "seek",
    "version": "1.0.8",
    "description": "Obsidian plugin for on-device hybrid semantic and lexical vault search.",
    "main": "main.js",
    "scripts": {
        "dev": "node esbuild.config.mjs",
        "build": "node esbuild.config.mjs production",
        "typecheck": "tsc --noEmit",
        "test": "vitest run",
        "bench": "npm run bench:1000 && npm run bench:5000",
        "bench:1000": "NODE_OPTIONS='--max-old-space-size=4096' SEEK_BENCH_SIZE=1000 vitest bench --run",
        "bench:5000": "NODE_OPTIONS='--max-old-space-size=8192' SEEK_BENCH_SIZE=5000 vitest bench --run",
        "bench:full": "SEEK_BENCH_FULL=1 NODE_OPTIONS='--max-old-space-size=8192' vitest bench --run",
        "version": "node version-bump.mjs && git add manifest.json versions.json"
    },
    "keywords": [
        "obsidian",
        "semantic-search",
        "embeddings",
        "transformers.js"
    ],
    "author": "Ryan Manor",
    "license": "MIT",
    "devDependencies": {
        "@types/node": "^22.0.0",
        "esbuild": "^0.24.0",
        "fake-indexeddb": "^6.2.5",
        "obsidian": "^1.7.2",
        "typescript": "^5.6.0",
        "vitest": "^2.1.0",
        "yaml": "^2.9.0"
    },
    "dependencies": {
        "minisearch": "^7.2.0"
    }
}
