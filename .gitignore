node_modules/
main.js
main.js.map
*.log
.DS_Store
.env
.env.*
!.env.example
data.json
.idea/
.vscode/

# Local perf/diagnostic captures (powermetrics dumps, procs.tsv, build backups).
# Kept locally for analysis; too large + throwaway to belong in history.
captures/

# Throwaway diagnostic probes / scratch HTML (e.g. lazyrelease-probe). Local-only.
experiments/

# Temp artifacts (screenshots, debug dumps, temp scripts)
.tmp/

# Seek diagnostic JSON (vault-style artifacts dir + repo probe copies). Summary .md may be kept.
.seek-artifacts/
.cursor/artifacts/
.cursor/seek-report.json
.cursor/gate-trace.jsonl
.cursor/scorecards/
.cursor/telemetry-screenshots/
.cursor/functional-traces/
.cursor/handoff/
# Canvas export pipeline (local only; canvas .tsx lives in Cursor managed folder)
.cursor/canvas-baseline-export.json
.cursor/canvas-baseline-snippet.json
.cursor/canvas-ts-block.txt
.cursor/playbook-run-results.json
.cursor/playbook-run-failures.md
.cursor/skills/seek-playbook-catalog/fixtures/generated/
